#include main.h

/**
 * main- prints _putchar followed by a new line
 * Return: Always 0
 */

int main(void)
{
	char words[8] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r'};

	unsigned int i;

	for (i = 0; i < sizeof(word[i]); i++)
	{
		_putchar(word[i]);
	}
	_putchar('\n');

	return (0)
}
