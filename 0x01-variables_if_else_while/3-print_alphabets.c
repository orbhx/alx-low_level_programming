#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alphalower = 'a';

	while (alphalower <= 'z';
	       {
		       putchar (alphalower);
			++alphalower;
	       }
	putchar('\n');

	return (0);
}
